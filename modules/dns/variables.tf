#variable "zone_name" {
#  description = "The DNS zone name (e.g. dev.sisense.example.com)"
  #type        = string
#}

#variable "env" {
  #description = "Environment name (e.g., dev, prod)"
  #type        = string
#}

/* variable "tags" {
  description = "Global tags to apply to all resources"
  type        = map(string)
} */
